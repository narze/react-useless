import { useState } from "react"
import { DefinitelyNotAny } from "./types"

export function useDittoConga() {
  if (window != null && window != undefined) {
    window.open("https://matias.ma/nsfw/")
  }
}
